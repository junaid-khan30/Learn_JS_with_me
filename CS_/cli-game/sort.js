var scores = [99, 101, 1, 0, -123, 90, 1];

var cohort = ["prash", "abhinav", "sam", "sam", "adnan", "ritvik", "sania", "yaseera", "hanzala", "junaid"];
console.log(scores.sort(function (a, b) { return b - a })); //Sort takes UTF(ASCII) as refernce
console.log(cohort.sort()); //Sort takes UTF(ASCII) as refernce