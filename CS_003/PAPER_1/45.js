
var abc = {};
var tet = [1, 0];
console.log(typeof abc);
console.log(typeof tet);
function xyz(){

}
console.log(typeof xyz);

/*
console.log(typeof abc);: Outputs the type of the variable abc, which is an empty object {}, so it prints object.
console.log(typeof tet);: Outputs the type of the variable tet, which is an array [1, 0], so it prints object. In JavaScript, arrays are a type of object.
console.log(typeof xyz);: Outputs the type of the function xyz, which is a function declaration, so it prints function. In JavaScript, functions are a distinct type.
*/